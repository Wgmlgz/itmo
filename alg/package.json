{
  "name": "alg",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "concurrently \"nodemon -e cpp -w . --exec .\\sus.bat\"",
    "dev": "concurrently \"nodemon -e cpp -w . --exec sus.bat\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  }
}
